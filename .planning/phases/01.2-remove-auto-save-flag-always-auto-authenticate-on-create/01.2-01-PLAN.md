---
phase: 01.2-remove-auto-save-flag-always-auto-authenticate-on-create
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/create.go
  - pkg/manager/manager.go
autonomous: true

must_haves:
  truths:
    - "gctx create <account> <project> auto-authenticates without any flag"
    - "gctx create --help does not show --auto-save flag"
    - "gctx create --help example text shows simple usage without flags"
    - "No dead code remains (no manual-flow branch, no autoSave variable, no init() flag registration)"
  artifacts:
    - path: "cmd/create.go"
      provides: "Cobra create command without --auto-save flag"
      contains: "m.CreateAccount(args[0], args[1])"
    - path: "pkg/manager/manager.go"
      provides: "CreateAccount with no autoSave parameter"
      contains: "func (m *Manager) CreateAccount(name, projectID string) error"
  key_links:
    - from: "cmd/create.go"
      to: "pkg/manager/manager.go"
      via: "CreateAccount call signature"
      pattern: "m\\.CreateAccount\\(args\\[0\\], args\\[1\\]\\)"
---

<objective>
Remove the `--auto-save` flag from `gctx create` and make auto-authentication the default (and only) behavior on account creation.

Purpose: Simplify UX -- every `gctx create` should authenticate and save ADC automatically. The flag adds friction with no real benefit since users always want credentials saved.
Output: Updated `cmd/create.go` and `pkg/manager/manager.go` with clean removal of the autoSave parameter and dead code.
</objective>

<execution_context>
@C:\Users\k0wl0\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\k0wl0\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@cmd/create.go
@pkg/manager/manager.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove autoSave parameter from CreateAccount in manager.go</name>
  <files>pkg/manager/manager.go</files>
  <action>
Modify `CreateAccount` method in `pkg/manager/manager.go`:

1. Change signature from `func (m *Manager) CreateAccount(name, projectID string, autoSave bool) error` to `func (m *Manager) CreateAccount(name, projectID string) error`.

2. Update the doc comment from "creates a new account with optional auto-save" to "creates a new account and runs authentication".

3. In the `SetProject` error handling block (lines 82-93), remove the `if autoSave` conditional. Keep ONLY the warn-and-continue path:
   ```go
   if err := gcloud.SetProject(projectID); err != nil {
       fmt.Printf("Warning: Failed to set project ID (%v). Proceeding to authentication...\n", err)
   } else {
       fmt.Printf("Set project: %s\n", projectID)
   }
   ```

4. Remove the `if autoSave` conditional block at lines 108-118. Replace with a direct call:
   ```go
   return m.autoSaveFlow(name)
   ```
   Delete the entire manual-flow `fmt.Println` instructions block (lines 113-117) -- this code path is now dead.

5. Verify no other references to `autoSave` remain in this file.
  </action>
  <verify>Run `go build ./...` from repo root -- must compile successfully. Run `grep -r "autoSave" pkg/` -- must return no results.</verify>
  <done>CreateAccount takes exactly 2 parameters (name, projectID), always calls autoSaveFlow, no manual-flow branch exists, no autoSave references in pkg/.</done>
</task>

<task type="auto">
  <name>Task 2: Remove --auto-save flag from create command in cmd/create.go</name>
  <files>cmd/create.go</files>
  <action>
Modify `cmd/create.go`:

1. Remove the package-level `var autoSave bool` declaration (line 8).

2. Update the `Example` string in the Cobra command to remove the `--auto-save` variant. Replace with:
   ```go
   Example: `  # Create a new account and auto-authenticate
     gctx create my-account my-project-id`,
   ```

3. Update the `RunE` function call from `m.CreateAccount(args[0], args[1], autoSave)` to `m.CreateAccount(args[0], args[1])`.

4. Delete the entire `func init()` block (lines 29-32) -- it only registers the `--auto-save` flag and is no longer needed.

5. Verify the import block still only needs `cobra` and `manager` (no unused imports).
  </action>
  <verify>Run `go build ./...` from repo root -- must compile successfully. Run `go vet ./...` -- must pass with no issues. Run `grep -r "auto.save\|auto-save\|autoSave" cmd/` -- must return no results.</verify>
  <done>cmd/create.go has no autoSave variable, no init() function, no --auto-save flag, updated example text, and calls CreateAccount with 2 args.</done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors
2. `go vet ./...` passes cleanly
3. `grep -rn "autoSave\|auto-save\|auto_save" cmd/ pkg/` returns no matches
4. `go run main.go create --help` shows no `--auto-save` flag and updated example text
</verification>

<success_criteria>
- `gctx create` no longer accepts `--auto-save` flag
- `gctx create <account> <project>` always auto-authenticates (calls autoSaveFlow)
- No dead code: no manual-instruction branch, no autoSave variable, no init() flag registration
- Code compiles and passes vet
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-remove-auto-save-flag-always-auto-authenticate-on-create/01.2-01-SUMMARY.md`
</output>
