---
phase: 01.1-fix-gctx-login-switch-and-shell-command-bugs
plan: 02
type: execute
wave: 2
depends_on:
  - "01.1-01"
files_modified:
  - gctx
autonomous: true

must_haves:
  truths:
    - "The gctx binary reflects the fixes from plan 01 (built after code changes)"
    - "gctx switch <account> succeeds when no ADC is saved and prints the login warning"
    - "gctx shell <account> exits with a clear error on Windows rather than 'no such file: /bin/sh'"
    - "gctx login (already fixed in source) works correctly in the new binary"
  artifacts:
    - path: "gctx"
      provides: "Rebuilt binary incorporating all three bug fixes"
  key_links:
    - from: "main.go"
      to: "pkg/manager/manager.go"
      via: "go build -o gctx main.go"
      pattern: "go build"
---

<objective>
Rebuild the gctx binary after the code fixes in plan 01 so all three bug fixes are reflected in the executable.

Purpose: The existing binary was built on 2026-01-05 and does not include the Login fix already in source, nor the two fixes applied in plan 01. A fresh build is required for any of the changes to take effect.
Output: A freshly built gctx binary at the project root.
</objective>

<execution_context>
@C:\Users\k0wl0\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\k0wl0\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-fix-gctx-login-switch-and-shell-command-bugs/01.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild gctx binary</name>
  <files>gctx</files>
  <action>
From the project root (C:\Users\k0wl0\OneDrive\Documents\dev\gctx), run:

```
go build -o gctx main.go
```

This is the same command used by `task build` in Taskfile.yml. Do not use `task build` directly — run `go build` explicitly so the output is visible and any compile errors surface clearly.

If the build succeeds, the file `gctx` (or `gctx.exe` on Windows) will be updated at the project root with a new modification timestamp.
  </action>
  <verify>
Run: go build -o gctx main.go
Expected: exits with code 0, no output (Go build is silent on success).

Then confirm the binary is fresh:
- On Unix: ls -la gctx — modification time should be today (2026-04-01)
- On Windows: ls -la gctx.exe or dir gctx.exe — same check

Run: ./gctx --version (or ./gctx.exe --version on Windows)
Expected: version output without error, confirming the binary runs.
  </verify>
  <done>
go build exits 0. The gctx binary exists at the project root with a modification timestamp of today. ./gctx --version (or --help) executes without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all three fixes are present in the binary</name>
  <files></files>
  <action>
Run the following smoke tests to confirm each fix is active in the new binary. These tests verify behavior without requiring actual GCP credentials.

Fix 1 (Login — already in source, now in binary):
- No direct CLI test possible without credentials, but go vet ./... should pass.

Fix 2 (Switch — non-fatal ADC):
If a test account exists in config, run:
  ./gctx switch <any-account-name>
Expected outcomes (one of):
  a) Switches successfully if ADC exists
  b) Prints "Warning: No saved ADC credentials for account '<name>'" and then "Switched to account: ..." if ADC is missing
  c) Prints "account not found" if the account name doesn't exist in config — this is correct behavior, not a bug

The key result to check: switch must NOT print "no saved ADC for account: <name>" and exit with a non-zero code. If it did, the fix was not applied.

Fix 3 (Shell — Windows):
  ./gctx shell <any-account-name>
Expected on Windows: shell launches (pwsh, powershell, or cmd) OR prints "account not found" — it must NOT print "fork/exec /bin/sh: no such file or directory".

Run: go vet ./...
Expected: no output (no vet warnings).
  </action>
  <verify>
go vet ./... exits 0 with no output.
Binary builds and runs.
  </verify>
  <done>
go vet ./... passes. Binary is rebuilt. If test accounts are available, switch behavior matches the expected outcomes above. The /bin/sh hardcode error does not appear on Windows when running shell command.
  </done>
</task>

</tasks>

<verification>
Final phase-level checks:
1. go build -o gctx main.go — exits 0
2. go vet ./... — exits 0, no warnings
3. ./gctx --help — displays help without error
4. Binary modification time is today (confirms rebuild happened)
5. Phase goal met: gctx is fully functional cross-platform for login, switch, and shell commands
</verification>

<success_criteria>
- gctx binary rebuilds successfully from updated source
- go vet ./... reports no issues
- Binary runs and displays help
- All three fixes are compiled into the binary:
  Fix 1: Login fix (was already in source, now in binary)
  Fix 2: Switch no longer hard-fails on missing ADC
  Fix 3: Shell uses cross-platform detection on Windows
</success_criteria>

<output>
After completion, create .planning/phases/01.1-fix-gctx-login-switch-and-shell-command-bugs/01.1-02-SUMMARY.md
</output>
