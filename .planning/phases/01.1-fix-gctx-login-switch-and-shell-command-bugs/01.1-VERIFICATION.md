---
phase: 01.1-fix-gctx-login-switch-and-shell-command-bugs
verified: 2026-04-01T15:30:00+07:00
status: passed
score: 4/4 must-haves verified
---

# Phase 01.1: Fix gctx Login, Switch and Shell Command Bugs — Verification Report

**Phase Goal:** Resolve critical Windows-blocking bugs in shell command, account switching, and login flow so gctx is fully functional cross-platform.
**Verified:** 2026-04-01T15:30:00+07:00
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                        | Status     | Evidence                                                                                       |
|----|------------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------------|
| 1  | gctx shell launches a shell on Windows without 'no such file' error          | VERIFIED   | StartShell() line 369: `if runtime.GOOS == "windows"` — 4-branch detection (pwsh/ps/COMSPEC/cmd.exe) |
| 2  | gctx switch succeeds even when no ADC has been saved                         | VERIFIED   | SwitchAccount() lines 222-225: RestoreADC failure prints warning, no `return err`             |
| 3  | After gctx switch, gctx active returns correct account name                  | VERIFIED   | SetActive(name) at line 243 is unconditional — reached regardless of ADC restore outcome      |
| 4  | The gctx binary reflects the fixes (built after code changes)                | VERIFIED   | Binary at project root, timestamp 2026-04-01 15:25 (today)                                   |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                     | Expected                                              | Status     | Details                                                     |
|------------------------------|-------------------------------------------------------|------------|-------------------------------------------------------------|
| `pkg/manager/manager.go`     | Contains `runtime.GOOS`                               | VERIFIED   | Line 369: `if runtime.GOOS == "windows"`                    |
| `pkg/manager/manager.go`     | SwitchAccount() warns on ADC fail, does not return    | VERIFIED   | Lines 222-225: warning printf, no `return err` in that block |
| `gctx` (project root binary) | Exists and was built today                            | VERIFIED   | 5,768,192 bytes, timestamp 2026-04-01 15:25                 |

### Key Link Verification

| From                                  | To                         | Via                                     | Status   | Details                                                                    |
|---------------------------------------|----------------------------|-----------------------------------------|----------|---------------------------------------------------------------------------|
| `StartShell()` shell detection        | `exec.Command(shell)`      | `runtime.GOOS == "windows"` branch      | WIRED    | Windows path resolves pwsh.exe/powershell.exe/COMSPEC/cmd.exe (lines 369-382) |
| `SwitchAccount()` ADC restore block   | `m.config.SetActive(name)` | Non-fatal warn-and-continue ADC restore | WIRED    | RestoreADC error block ends at line 225; SetActive called at line 243 unconditionally |

### Requirements Coverage

| Requirement                                     | Status    | Blocking Issue |
|-------------------------------------------------|-----------|----------------|
| Windows shell command no longer fails           | SATISFIED | None           |
| Account switching works without saved ADC       | SATISFIED | None           |
| Active account name correct after switch        | SATISFIED | None           |

### Anti-Patterns Found

No blocker anti-patterns. No TODO/FIXME/placeholder comments in the modified functions.

Note: The `autoSaveFlow()` function contains design-comment lines (lines 152-162) that narrate implementation decisions. These are documentation, not stubs, and do not affect correctness.

### Human Verification Required

The following behaviors require a live gcloud environment to test end-to-end, but the structural code verification is complete:

1. **Shell launch on Windows**
   - Test: Run `gctx shell <account>` on Windows where `$SHELL` is unset
   - Expected: A PowerShell or cmd.exe prompt opens without "no such file" error
   - Why human: Requires Windows runtime with gctx account configured

2. **Switch without saved ADC**
   - Test: Run `gctx switch <account>` for an account that has no ADC file saved
   - Expected: Warning message printed, command completes, `gctx active` returns the account name
   - Why human: Requires a configured account with no ADC on disk to exercise the error path

## Verification Detail

### Truth 1 — Windows shell detection

`StartShell()` at line 367-383 of `pkg/manager/manager.go`:

- `$SHELL` env var is checked first (preserves Unix behavior)
- When empty AND `runtime.GOOS == "windows"`: tries `pwsh.exe` via `exec.LookPath`, then `powershell.exe`, then `$COMSPEC`, then hardcodes `cmd.exe`
- When empty AND NOT windows: falls back to `/bin/sh`
- `runtime` is present in the import block (line 7)
- Package compiles cleanly: `go build ./pkg/manager/` exits 0

### Truth 2 — Non-fatal ADC restore in SwitchAccount()

`SwitchAccount()` at lines 215-247:

- `adc.RestoreADC(name)` error block (lines 222-225) contains only `fmt.Printf` calls
- No `return err` inside the ADC error block — confirmed by reading the full function and by grep showing no `return err` in that region
- Execution continues to `gcloud.ActivateConfig()`, `gcloud.SetProject()`, and `m.config.SetActive(name)`

### Truth 3 — SetActive() called unconditionally

`m.config.SetActive(name)` at line 243 is outside all conditional blocks. It executes in every code path where account lookup succeeds, regardless of ADC restore or SetProject outcomes. Both of those failures are handled as warnings (fmt.Printf only).

### Truth 4 — Binary currency

Binary `/c/Users/k0wl0/OneDrive/Documents/dev/gctx/gctx` exists with mtime `2026-04-01 15:25:05`, confirming it was built after the code changes were applied today.

---

_Verified: 2026-04-01T15:30:00+07:00_
_Verifier: Claude (gsd-verifier)_
